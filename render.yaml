services:
  # PHP Backend Service
  - type: web
    name: churchtrack-api
    env: docker
    dockerfilePath: ./Dockerfile
    # Using external Aiven database - environment variables set in Render dashboard
    # envVars are configured manually in Render dashboard, not from render.yaml
